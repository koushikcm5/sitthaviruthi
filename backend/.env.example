# Database Configuration
DB_URL=jdbc:mysql://localhost:3306/yoga_attendance?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=your_database_password

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_at_least_64_characters_long_for_production_security
JWT_EXPIRATION=3600000
JWT_REFRESH_EXPIRATION=604800000

# Email Configuration
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password
MAIL_DEBUG=false

# Server Configuration
PORT=8080
SHOW_SQL=false

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# FCM Configuration
FCM_CREDENTIALS_PATH=path/to/firebase-credentials.json
FCM_ENABLED=false
